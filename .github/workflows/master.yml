name: Production Deployment

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up SSH
        uses: appleboy/ssh-action@master
        with:
          username: ubuntu
          key: ${{ secrets.SSH_KEY }}
          host: ec2-34-238-254-200.compute-1.amazonaws.com
          port: 22
          script: |
            cd /home/Sabari/sabari7868
            sudo docker build -t ghj .

